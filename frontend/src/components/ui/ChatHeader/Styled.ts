import styled from "@emotion/styled";

export const StyledChatHeader = styled.div`
    display: flex;
    min-height: 60px;
    align-items: center;
    justify-content: space-between;
`

export const StyledChatHeaderTitle = styled.div`
    font-size: 1.5em;
    font-weight: bold;
`;

export const StyledActionsContainer = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    margin-right: 24px;
`;